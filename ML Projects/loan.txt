this project is about the credit wise loan system in this we will approving the loan for those who will nfullfill the conditions
.simpleimputer 
    handle the missing values with simple stratagies

Exploratory Data Analysis
    Understand the Data
    Identify Missing Values
    Detect Outliers
    Analyze Data Distribution
    Find Relationships Between Variables
    Detect Duplicate Records


Encoding
    1.LableEncoder(convert into binary number like male=1, female=0,used of ordinal data(jaha per data order me ho))
    2.OneHotEncoder(create bimnary colun for every data(nominal data(jis data ka order na ho)))



correlation heatmap
visual representaion between the the numerical Variables   and the dataset 
jitna jada postive values utna jadfda positive correlation
jitna jadaa negative values utna jada negative correlation



trainig and evaluate the model
we have to reducing the false positive(for this we are using the precision)
on the second number we give the priority to our reaclll
because it focus on "hamne kitne ache costumer ko lose kar diya hai"


best model after applying the multiple models (knn,logistic regression , naive bayse models)
    best model is the naive bayse because it have highest precision score
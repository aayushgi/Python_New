where clause: used to specify condition in sql query
1)select record of employee with empid 1001
	select * from employee where empid=1001;

2)select empid,empname from employee table with empid 1001
    select empid, empname from employee whre empid=1001

DELETE:it is used to delete record from table
syntax:
    delete from <table name> where <condition>
    eg:
        delete record from employee table with empid1001
        delete from employee where empid=1001;

UPDATE:used to modify record of table
syntax:
    update <table_name> set <column_name>=<value>, <column_name>=<value> whre <condition>;
    e.g
        update employee set salary=60000 where empid=1002;


TRUNCATE:used to delete all record from table but structure remains
syntax:
    truncate table<table_name>;
    e.g
        truncate table employee;

DROP:it is used to delete database object
syntax:
    drop table<table_name>
    eg:
        drop table employee;


ALTER:it is used to modify structure of database object

create table login
(
    userid varchar(50) primary key,
    password varchar(50)
);

1.use of alter command to add a new column
syntax:
    alter table <table_name> add <column_name> <data_type>
    eg:
        alter table login add usertype varchar(50);

2. use of alter command to drop a column
syntax:
    alter table<table_name> drop column <column_name>
    eg  
        alter table login drop column usertype;

3.use of alter command to modify column
syntax:
    alter table <table_name> modify <column_name> <new_data_type>;
    eg:
        alter table login modify password varchar(30);

4.use of alter command to rename the column
syntax:
    alter table <table_name> change <column_old_name> <column_new_name> <data_type>;
    eg:
        alter table  login change password pwd varchar(30);

5.use of alter command to add new column after a given column
syntax:
    alter table <table_name> add <new_column_name> <data_type> after <given_column_name>;
    eg:
        alter table login add usertype varchar(50) after userid;        




_________________________________________________________________________________________________________
5/feb/2026


create table empinfo(
    empid int primary key,
    empname varchar(50),
    department varchar(50),
    salary bigint
);

insert into empinfo values(1001,'ravi singh','devlopment',500000);
insert into empinfo values(1002,'priya singh','HR',600000);
insert into empinfo values(1003,'ajay mishra','devlopment',550000);
insert into empinfo values(1004,'aman singh','testing',450000);

ORDER BY:used to sort records of table in ascending or descending order 

1)sort empinfo table in ascending order based on empname
select * from empinfo order by empname;
2)sort empinfo table in descending order based on empname
select * from empinfo order by empname desc;

SQL OPERATORS:
Arithmatic operators:
+ addition 
- substraction
* multiplication
/ division
% modulo(to calculating remainder)

comparision operators:
> grater than
< less than
>= graterthan or equal
<= lessthan equal
= equal
<> not equal

logical operators:
AND 
OR 
NOT

special operators: 
like:used to pattern matching
select * from empinfo where empname like '%singh';

between:select the values in given range
select * from empinfo where salary between 500000 and 550000;

in:in operators is used to check value in given values


_________________________________________________________________________________________________________
7/feb/2026

JOIN OPERATION: select data from two or more than two table then you can perform join operation.
for join operation atleast one column should be same in both table.
FORIGEN KEY: works as a primary key in another table used to establish realationship between two table

table name: staff
column name  |   data type  | constraint
empid        |   int        | p.key
empname      |  varchar(50) | 

table name: product
column name  |   data type  | constraint
pid          |   int        | p.key
pname        |  varchar(50) |
empid        |   int        | f.key(referenced to staff)



create table staff(
    empid int primary key
    empname varchar(50)
); 


create table product(
    pid int primary key,
    pname varchar(50),
    empid int,
    foreign key(empid) references staff(empid)
);


staff 
insert into staff values(1001,'jhon');
insert into staff values(1002,'brown');
insert into staff values(1003,'green');
insert into staff values(1004,'lily');

empid    |   empname
1001     |    jhon
1002     |    brown
1003     |    green
1004     |    lily

product
insert into product values(101,'printer',1001);
insert into product values(102,'scanner',1002);
insert into product values(103,'plotter',1002);
insert into product values(104,'laptop',1003);
insert into product values(105,'projector','Null');



pid      |        pname     |      empid
101      |   printer        |    1001
102      |   scanner        |    1002
103      |   plotter        |    1002  
104      |   laptop         |    1003 
105      |   projector      |    'Null'


Natural join: when we perform this operation common records from both are display


task: we want to select empname from staff table and pname from product table on the basis of empid

select staff.empname, product.pname from staff, product where staff.empid=product.empid;

LEFT JOIN:
all records pf left table are display and matching records of right table are display

select staff.empname, product.pname from staff left join product on staff.empid=product.empid; 


Right join: all records pf right table are display and matching records of left table are display
select staff.empname, product.pname from staff right join product on staff.empid=product.empid; 


Full join: all records of both table are display. it is like as union of left join and right join
select staff.empname, product.pname from staff left join product on staff.empid=product.empid 
union
select staff.empname, product.pname from staff right join product on staff.empid=product.empid; 